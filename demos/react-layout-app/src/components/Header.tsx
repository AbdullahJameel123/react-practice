import { Container } from "react-bootstrap";

const Header= () => {
  return (
    <header className="bg-dark text-light py-3 shadow-lg">
      <Container className="d-flex justify-content-between align-items-center">
      </Container>
    </header>
  );
};

export default Header;
