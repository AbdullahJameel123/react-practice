import { Container } from "react-bootstrap";

const Home = () => {
  return (
    <Container className="text-light py-5">
      <h2 className="fw-bold text-primary glow-text">Welcome to the React App</h2>
      <p>
        This is a beautifully styled React website using React + Bootstrap + TypeScript.
      </p>
    </Container>
  );
};

export default Home;
